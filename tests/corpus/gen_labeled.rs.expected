/// @java L
#[derive(Clone, Default)]
struct L {
}

impl L {

    /// @java L#m
    fn m(&self)   {
        'outer:  {
             let mut i: i32 = 0;
            while i < 3 {
                {
                    break 'outer;
                }
                i += 1;
             }
         }

    }

    pub fn new() -> Self { Default::default() }
}

